#define in 9
#define in 8
#define in 7
#define in 6
#define int m1_speed = 80; // speed of motor 1
#define int m3_speed = 80; // speed of motor 2
#define int left_rotation_speed = 250; // speed of left rotation
#define int right_rotation_speed = 250; // speed of roght rotation


void setup() {
  pinmod(in 1, OUTPUT);
  pinmod(in 2, OUTPUT);
  pinmod(in 3, OUTPUT);
  pinmod(in 4, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:

}
